
// Define the range B2:B10
                    const range = XLSX.utils.decode_range('B2:B10');
                    const values = [];
                    
                    // Extract values from the range
                    for (let rowNum = range.s.r; rowNum <= range.e.r; rowNum++) {
                        const cellAddress = { c: range.s.c, r: rowNum };
                        const cellRef = XLSX.utils.encode_cell(cellAddress);
                        const cell = firstSheet[cellRef];
                        
                        // Get formatted value (cell.w) or raw value (cell.v)
                        values.push(cell ? (cell.w || cell.v) : '');
                    }








                    

      const range = XLSX.utils.decode_range('B2:B115');
      const values = [];

      // Extract values from the range
      for (let rowNum = range.s.r; rowNum <= range.e.r; rowNum++) {
        const cellAddress = { c: range.s.c, r: rowNum };
        const cellRef = XLSX.utils.encode_cell(cellAddress);
        const cell = sheet[cellRef];

        // Get formatted value (cell.w) or raw value (cell.v)
        values.push(cell ? (cell.w || cell.v) : '');
      }